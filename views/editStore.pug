extends layout

include mixins/_storeForm

block content
    .inner
        //- h2 You are on page '#{title}'
        //- pre= h.dump(store)
        h2= title
        //- using the mixin:
        //- +storeForm({ name: 'Silpo'})
        +storeForm(store)